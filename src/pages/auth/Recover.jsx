import Header from "@features/auth/recover/header";
import Body from "@features/auth/recover/body";
import Form from "@features/auth/recover/form";
import Footer from "@features/auth/recover/footer";

const Recover = () => {
    return (
        <>
            <Header />
            <Body>
                <Form />
            </Body>
            <Footer />
        </>
    );
};

export default Recover;
